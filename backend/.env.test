# Test Environment Configuration
NODE_ENV=test
PORT=3002

# Test Database Configuration (using development database for now)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=credit_card_recommendations
DB_USER=username
DB_PASSWORD=password

# Test Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Test JWT Configuration
JWT_SECRET=test-jwt-secret-key
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=1d

# Mock External APIs for testing
PLAID_CLIENT_ID=test-plaid-client-id
PLAID_SECRET=test-plaid-secret
PLAID_ENV=sandbox

RECENGINE_API_URL=http://localhost:8001
RECENGINE_API_KEY=test-recengine-api-key

SENDGRID_API_KEY=test-sendgrid-api-key
FROM_EMAIL=test@example.com